<%= form_for(@xml) do |f| %>
  <% if @xml.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@xml.errors.count, "error") %> prohibited this xml from being saved:</h2>

      <ul>
      <% @xml.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :key_access %><br>
    <%= f.text_field :key_access, :readonly=>true, :size=>"70" %>
  </div>
  <div class="field">
    <%= f.label :xml_text %><br>
    <%= f.text_area :xml_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
